
<div>
  <%= image_tag @product.photo, width: 200  if @product.photo.attached? %>
</div>

<h2 class="title"><%= @product.title %></h2>
<p class="description"><%= @product.description %></p>
<p class="price"><%= @product.price %>€</p>

<p><%= link_to t('.edit'), edit_product_path(@product) if @product.user_id == Current.user&.id %></p>
<p><%= link_to t('.delete'), product_path(@product), data: { turbo_method: :delete, turbo_confirm: t('common.confirm_message') } if @product.user_id == Current.user&.id %></p>
<!-- Por defecto Rails usa el método HTTP GET por defecto. Para borrar un producto necesito el método DELETE. Tengo que declararlo. Rails 7 usa Turbo para hacerlo. -->

<p><%= link_to t('common.back'), products_path %></p>
